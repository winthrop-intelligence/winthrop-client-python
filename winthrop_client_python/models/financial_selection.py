# coding: utf-8

"""
Winthrop Intelligence Internal API

No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

The version of the OpenAPI document: 1.54.0-dev.10377
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from winthrop_client_python.models.financial_selection_result import (
    FinancialSelectionResult,
)
from typing import Optional, Set
from typing_extensions import Self


class FinancialSelection(BaseModel):
    """
    FinancialSelection
    """  # noqa: E501

    school_id: StrictInt
    source: StrictStr = Field(
        description="The requested mode (eada, ncaa_frs, best_available, or both) — not necessarily every result's actual resolved source; see each result's own source."
    )
    grain: StrictStr
    requested_year: Optional[StrictInt]
    results: List[FinancialSelectionResult] = Field(
        description="One Result per source actually shown — one entry for eada/ncaa_frs/ best_available modes, up to two under both."
    )
    __properties: ClassVar[List[str]] = [
        "school_id",
        "source",
        "grain",
        "requested_year",
        "results",
    ]

    @field_validator("source")
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["eada", "ncaa_frs", "best_available", "both"]):
            raise ValueError(
                "must be one of enum values ('eada', 'ncaa_frs', 'best_available', 'both')"
            )
        return value

    @field_validator("grain")
    def grain_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["institution", "sport"]):
            raise ValueError("must be one of enum values ('institution', 'sport')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FinancialSelection from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in results (list)
        _items = []
        if self.results:
            for _item_results in self.results:
                if _item_results:
                    _items.append(_item_results.to_dict())
            _dict["results"] = _items
        # set to None if requested_year (nullable) is None
        # and model_fields_set contains the field
        if self.requested_year is None and "requested_year" in self.model_fields_set:
            _dict["requested_year"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FinancialSelection from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "school_id": obj.get("school_id"),
                "source": obj.get("source"),
                "grain": obj.get("grain"),
                "requested_year": obj.get("requested_year"),
                "results": (
                    [
                        FinancialSelectionResult.from_dict(_item)
                        for _item in obj["results"]
                    ]
                    if obj.get("results") is not None
                    else None
                ),
            }
        )
        return _obj
