# coding: utf-8

"""
Winthrop Intelligence Internal API

No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

The version of the OpenAPI document: 1.54.0-dev.10366
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictBool,
    StrictInt,
    StrictStr,
    field_validator,
)
from typing import Any, ClassVar, Dict, List, Optional
from winthrop_client_python.models.financial_selection_metric import (
    FinancialSelectionMetric,
)
from typing import Optional, Set
from typing_extensions import Self


class FinancialSelectionResult(BaseModel):
    """
    One source's resolved report for the requested school/grain/year (Financials::SourceSelection::Result) — never a row assembled from unrelated line items across two reports.
    """  # noqa: E501

    source: StrictStr
    year: Optional[StrictInt] = None
    grain: StrictStr
    available: StrictBool = Field(
        description="False when no report exists for this source/year/grain, the report exists but has nothing actually reported (NCAA/FRS only), no sport-code crosswalk exists for this sport (EADA sport grain only), a requested metric filter matched nothing this source's vocabulary emits at all, or (single-source mode only) the viewer lacks the ability for this source on this school."
    )
    fallback_reason: Optional[StrictStr] = Field(
        default=None,
        description="sport_not_mapped_to_eada marks a WinAD sport with no EADA crosswalk row at all (a mapping gap, not a missing filing); source_not_permitted_for_viewer also covers best_available/both requests where the viewer holds neither source ability for this school.",
    )
    comparability_summary: StrictStr = Field(
        description="Most-conservative comparability_state across this result's metrics (not_comparable outranks comparison_only outranks mergeable; source_only is the fallback default)."
    )
    metrics: List[FinancialSelectionMetric]
    __properties: ClassVar[List[str]] = [
        "source",
        "year",
        "grain",
        "available",
        "fallback_reason",
        "comparability_summary",
        "metrics",
    ]

    @field_validator("source")
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["eada", "ncaa_frs", "none"]):
            raise ValueError("must be one of enum values ('eada', 'ncaa_frs', 'none')")
        return value

    @field_validator("grain")
    def grain_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["institution", "sport"]):
            raise ValueError("must be one of enum values ('institution', 'sport')")
        return value

    @field_validator("fallback_reason")
    def fallback_reason_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(
            [
                "ncaa_frs_unavailable_used_eada",
                "no_report_available_for_any_source",
                "source_not_permitted_for_viewer",
                "sport_not_mapped_to_eada",
            ]
        ):
            raise ValueError(
                "must be one of enum values ('ncaa_frs_unavailable_used_eada', 'no_report_available_for_any_source', 'source_not_permitted_for_viewer', 'sport_not_mapped_to_eada')"
            )
        return value

    @field_validator("comparability_summary")
    def comparability_summary_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(
            ["not_comparable", "comparison_only", "mergeable", "source_only"]
        ):
            raise ValueError(
                "must be one of enum values ('not_comparable', 'comparison_only', 'mergeable', 'source_only')"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FinancialSelectionResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in metrics (list)
        _items = []
        if self.metrics:
            for _item_metrics in self.metrics:
                if _item_metrics:
                    _items.append(_item_metrics.to_dict())
            _dict["metrics"] = _items
        # set to None if year (nullable) is None
        # and model_fields_set contains the field
        if self.year is None and "year" in self.model_fields_set:
            _dict["year"] = None

        # set to None if fallback_reason (nullable) is None
        # and model_fields_set contains the field
        if self.fallback_reason is None and "fallback_reason" in self.model_fields_set:
            _dict["fallback_reason"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FinancialSelectionResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "source": obj.get("source"),
                "year": obj.get("year"),
                "grain": obj.get("grain"),
                "available": obj.get("available"),
                "fallback_reason": obj.get("fallback_reason"),
                "comparability_summary": obj.get("comparability_summary"),
                "metrics": (
                    [
                        FinancialSelectionMetric.from_dict(_item)
                        for _item in obj["metrics"]
                    ]
                    if obj.get("metrics") is not None
                    else None
                ),
            }
        )
        return _obj
