# coding: utf-8

"""
Winthrop Intelligence Internal API

No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

The version of the OpenAPI document: 1.53.53-dev.10206
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from winthrop_client_python.models.job_post_disagreement import JobPostDisagreement
from typing import Optional, Set
from typing_extensions import Self


class JobPostDisagreementCollection(BaseModel):
    """
    JobPostDisagreementCollection
    """  # noqa: E501

    new: Optional[List[JobPostDisagreement]] = None
    new_truncated: Optional[StrictBool] = Field(
        default=None,
        description='True if new_page is not the last page for the "new" section.',
    )
    new_total_count: Optional[StrictInt] = Field(
        default=None, description='Total number of "new" rows across all pages.'
    )
    still_pending: Optional[List[JobPostDisagreement]] = None
    still_pending_truncated: Optional[StrictBool] = Field(
        default=None,
        description='True if still_pending_page is not the last page for the "still_pending" section.',
    )
    still_pending_total_count: Optional[StrictInt] = Field(
        default=None,
        description='Total number of "still_pending" rows across all pages.',
    )
    __properties: ClassVar[List[str]] = [
        "new",
        "new_truncated",
        "new_total_count",
        "still_pending",
        "still_pending_truncated",
        "still_pending_total_count",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of JobPostDisagreementCollection from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in new (list)
        _items = []
        if self.new:
            for _item_new in self.new:
                if _item_new:
                    _items.append(_item_new.to_dict())
            _dict["new"] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in still_pending (list)
        _items = []
        if self.still_pending:
            for _item_still_pending in self.still_pending:
                if _item_still_pending:
                    _items.append(_item_still_pending.to_dict())
            _dict["still_pending"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of JobPostDisagreementCollection from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "new": (
                    [JobPostDisagreement.from_dict(_item) for _item in obj["new"]]
                    if obj.get("new") is not None
                    else None
                ),
                "new_truncated": obj.get("new_truncated"),
                "new_total_count": obj.get("new_total_count"),
                "still_pending": (
                    [
                        JobPostDisagreement.from_dict(_item)
                        for _item in obj["still_pending"]
                    ]
                    if obj.get("still_pending") is not None
                    else None
                ),
                "still_pending_truncated": obj.get("still_pending_truncated"),
                "still_pending_total_count": obj.get("still_pending_total_count"),
            }
        )
        return _obj
